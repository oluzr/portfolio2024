export interface AppIconProp {
  imgUrl: string;
  iconName: string;
  linkTo?: string;
  style?: React.CSSProperties;
  notiNum?:number
}
export interface AppIconStyleProp {
  style?: React.CSSProperties;
}
